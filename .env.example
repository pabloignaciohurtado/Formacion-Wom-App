# Copia este archivo a .env y rellena los valores.
# .env está en .gitignore: nunca se commitea.
#
# En GitHub, los mismos valores van en
# Settings > Secrets and variables > Actions > Repository secrets.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
